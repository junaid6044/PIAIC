const numbers: number[] = [1, 2, 3, 4, 5];

const invalidIndex = 10;
console.log(`Accessing element at index ${invalidIndex}: ${numbers[invalidIndex]}`);

const validIndex = 2;
console.log(`Accessing element at index ${validIndex}: ${numbers[validIndex]}`);
